// TODO: Add MongoDB configuration and connection logic here

// TODO: Complete adding routes and logic for parsing out data to the 
// index files for handlebar -> also add the code to the handlebars templates
module.exports = (app) => {

    app.get('/', (req, res) => res.send('in root'));

}
